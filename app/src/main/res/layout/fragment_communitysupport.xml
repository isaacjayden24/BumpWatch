<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".Model.CommunitysupportFragment">

    <!-- Add your content here -->
    <TextView
        android:id="@+id/text_welcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Our Wellness Community!"
        android:textColor="@color/navy"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"/>

    <TextView
        android:id="@+id/text_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connect with other women on their wellness journey. Share experiences, tips, and support each other!"
        android:textSize="16sp"
        android:layout_below="@id/text_welcome"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="32dp"/>

    <!-- Join Community Button -->
    <Button
        android:id="@+id/btn_join_community"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Join Community"
        android:layout_below="@id/text_description"
        android:layout_marginTop="32dp"
        android:layout_marginHorizontal="32dp"
        android:backgroundTint="@color/cardview_dark_background"
        android:textColor="@android:color/white"
        android:textSize="18sp"/>

</RelativeLayout>

